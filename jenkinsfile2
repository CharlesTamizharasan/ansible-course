pipeline {
    agent any

    parameters {
        string(name: 'USERNAME', defaultValue: '', description: 'Enter the username to update in test.txt')
    }

    environment {
        GITHUB_TOKEN = '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'
        GITHUB_REPO = 'CharlesTamizharasan/ansible-course.git'
    }

    stages {
        stage('Update GitHub Repo') {
            steps {
                script {
                    git branch: 'jcloud-dev', url: 'https://github.com/CharlesTamizharasan/ansible-course.git'
                    // Set Git user details
                    sh 'git config user.name "CharlesTamizharasan"'
                    sh 'git config user.email "charlesit24@gmail.com"'
                    sh 'cat test.txt'
                    // Read the content of test.txt
                    def content = readFile('test.txt').trim()
                    // Update the USERNAME line in the content
                    content = content.replaceAll(/^USERNAME=.*/, "USERNAME=${params.USERNAME}")
                    // Write the updated content to a temporary file
                    def tempFile = writeFile file: 'test_temp.txt', text: content
                    // Use sed to replace the original file with the temporary file
                    sh "sed -i '' -e 's|^USERNAME=.*|USERNAME=${params.USERNAME}|' ${tempFile}"
                    sh 'cat test.txt'
                    // Git operations
                    sh 'git add .'
                    sh 'git commit -m "Update test.txt with username ${params.USERNAME}"'
                    sh 'git push origin main'
                }
            }
        }
    }
}
